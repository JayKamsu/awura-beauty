name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    name: Lint · Typecheck · Build
    runs-on: ubuntu-latest
    timeout-minutes: 20

    env:
      NEXT_TELEMETRY_DISABLED: "1"
      NEXT_PUBLIC_SITE_URL: https://awurabeauty.com
      NEXT_PUBLIC_CONTACT_EMAIL: Care@awurabeauty.com
      NEXT_PUBLIC_SUPABASE_URL: https://example.supabase.co
      NEXT_PUBLIC_SUPABASE_ANON_KEY: ci-anon-key
      SUPABASE_SERVICE_ROLE_KEY: ci-service-role-key
      ADMIN_DEV_BYPASS: "false"
      ADMIN_EMAILS: admin@awura.local
      SENTRY_AUTH_TOKEN: ""
      # Clés publishable factices pour le build CI (jamais utilisées en prod)
      NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY: pk_test_ci_placeholder
      NEXT_PUBLIC_PAYPAL_CLIENT_ID: ci_paypal_client_placeholder
      STRIPE_SECRET_KEY: sk_test_ci_placeholder
      PAYPAL_CLIENT_SECRET: ci_paypal_secret_placeholder
      PAYPAL_API_BASE: https://api-m.sandbox.paypal.com
      STRIPE_WEBHOOK_SECRET: whsec_ci_placeholder

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Typecheck
        run: npm run typecheck

      - name: Build
        run: npm run build
